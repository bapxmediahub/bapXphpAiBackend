# Booking Module

Owns remote astrology session requests.

Main files: `BookingController.php`, `AppointmentService.php`, `views/public/consult.php`, `views/account/bookings.php`.

Key checks: no dated slot picker is used, call/message requests save per-session rate and credits spent, and ended sessions can collect five-star reviews.
