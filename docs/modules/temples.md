# Temples Module

Owns temple catalog pages and admin temple records.

Main files: `TempleService.php`, `views/public/temples.php`, `views/public/temple.php`, admin resource views.

Key checks: temple list/detail pages render real data from MySQL, no placeholder temple cards are emitted, and configured external map links are safe.
